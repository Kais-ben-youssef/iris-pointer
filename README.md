# iris-pointer
## Onboarding

install `nodejs` + `npm` 

```bash
sudo apt-get install nodejs && \
sudo apt-get install npm 
```
## USE
CD the git repo then :
```bash
npm install && npm run start 
```
